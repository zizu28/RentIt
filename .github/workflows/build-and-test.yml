name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
        
    - name: Restore dependencies
      run: dotnet restore RentIt.slnx
      
    - name: Build
      run: dotnet build RentIt.slnx --no-restore
      
    - name: Test
      run: dotnet test RentIt.slnx --no-build --verbosity normal
